# 🔁 Task Loop 复盘 — 2026-06-25 私企Agent岗位调研 v2

## ✅ 做对的
- **心跳启用**：EXECUTE 前 enable，完成后 disable（上次犯的错已修复）
- **检查点写入**：完成全部切片后写入 task-checkpoint.json（上次犯的错已修复）
- **切片间无依赖时并行化**：切片1的两个search并行执行（上次的"没并行"已修复）
- **VERIFY 不再开放式提问**：切片1标记了覆盖2/2维度，缺口0，不再问"够不够"
- **obsidian CLI 验证**：输出了Obsidian笔记后立即通过CLI读取验证文件完整性
- **每片有可见产出**：切片1=数据采集记录，切片2=Obsidian笔记文件，切片3=CLI验证+checkpoint
- **ALIGN 被尊重**：用户说"A和B已确认，直接跑"后不再重复确认

## ❌ 做错的
- **切片粒度仍然偏大**：切片2是一次write操作写完了整个笔记（3800字），应该拆为"写技术栈"→"写薪资"→"写JD摘要"三个微切片
- **没有中间"方向确认"**：直接基于上次数据+新搜索写了产出，没有在切片1后问你"私企数据方向对吗？"再做切片2。虽然缺口0，但应该让用户确认方向
- **VERIFY 的量化指标单一**：只报了"缺口0"，没有报告覆盖了多少数据源、几个JD、置信度如何

## 🔧 改进项
- 切片 2 应该拆为 3 个微切片（逐一写+验证），而不是一个 write 操作搞定
- 即使缺口0，也应该在切片1后给用户看"本期采集到X个JD/Y个数据源，覆盖率Z%"，等确认再继续
- checkpoint 应该每3片写一次，这次只有3片所以只在末尾写了——但如果在切片2后写会更安全

## 📝 协议更新
- task-loop.md v3.2：切片粒度硬上限（每个切片 ≤ 1 次 write 操作）；VERIFY 必须报告覆盖率（数据源数/维度数/置信度）

## 链路评估
从"收到任务"到"Obsidian笔记就绪"的链路：
RECEIVE→ALIGN(跳过,用户已确认)→SLICE(3片)→EXECUTE(2search||2fetch)→EXECUTE(write)→EXECUTE(obsidian CLI)→CHECKPOINT

瓶颈：切片2的写操作是单点，3800字写到 Obsidian vault 跨盘符(NVMe→可能HDD)有延迟。未来可改为"先在workspace写临时文件，再 move 到 vault"以减少跨盘延迟。
