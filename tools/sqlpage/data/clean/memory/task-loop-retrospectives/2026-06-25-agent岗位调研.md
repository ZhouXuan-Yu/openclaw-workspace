# 🔁 Task Loop 复盘 — 2026-06-25 Agent岗位需求调研

## ✅ 做对的
- ALIGN 真的执行了（两轮：平台范围 + 侧重点），没有跳过
- 每个切片完成后有汇报 + 等用户确认
- 每片有独立输出文件，不是一口吃完
- 没有在切片1就跳去写结论

## ❌ 做错的
1. **心跳未启用**：长任务全程没有 enable cron task-heartbeat，违反 EXECUTE 规定
2. **VERIFY 太形式化**：问了"数据够不够？"但没给量化指标，用户无法判断。正确做法是报告覆盖率
3. **没有写 checkpoint**：完成3片后按规定必须写 checkpoint，我没有写
4. **切片粒度偏大**：切片1用5次工具调用（2 search + 3 fetch），应拆为更细粒度
5. **切片间无动态调整**：切片1发现反爬问题，切片2没有针对调整
6. **没有并行化**：切片1和切片2数据源独立，应该并行执行

## 🔧 改进项（已更新到 task-loop.md）
1. VERIFY 铁律：量化证据/标记缺口/决策建议，禁止开放式提问
2. 检查点强制规则：每3片必须写 checkpoint
3. 心跳强制规则：执行前enable，完成后disable
4. 切片设计原则：1-3工具调用/片，独立可验证，并行优先，动态调整
5. 复盘机制：每次Loop完成后自动生成此文件

## 📝 协议更新
task-loop.md v3.1：新增切片设计原则、VERIFY铁律、心跳强制规则、检查点强制规则、复盘机制
